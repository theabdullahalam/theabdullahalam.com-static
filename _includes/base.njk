<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    
    <link rel="stylesheet" href="/static/css/base.css">
    <link rel="stylesheet" href="{{ stylesheet }}">
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/showdown/2.1.0/showdown.min.js"></script>
    
</head>
<body class="override">
    <!-- the everything div is so that all the content can be centered in the page but everything insie is left aligned -->
    <div class="everything">
        <div class="header">
            <a href="/"><img src="/static/img/favicon.png" class="site-logo" /></a>
            <a href="/"><h1 class="site-title">theabdullahalam.com</h1></a>
            <button onclick="toggleSidebar()" class="toggle-sidebar">go to...</button>
        </div>
        <div class="content">
            {# {% block content %}{% endblock %} #}
            {{ content | safe }}
        </div>
        <div class="sidebar" id="sidebar">
            <div class="persistent">
                <a href="/posts/all/" class="nav-link {% if tag == 'all' %}selected{% endif %}">all posts</a>
            </div>

            <div class="tags">
                {% for _tag, posts in collections %}
                    {% if _tag not in ['all', 'post'] %}
                        {% if tags %}
                            <a 
                                href="/posts/{{ _tag | slugify }}/" 
                                class="nav-link {% if _tag in tags %}selected{% endif %}"
                            >
                                {{ _tag }}
                            </a>
                        {% else %}
                            <a 
                                href="/posts/{{ _tag | slugify }}/" 
                                class="nav-link {% if _tag == tag %}selected{% endif %}"
                            >
                                {{ _tag }}
                            </a>
                        {% endif %}
                    {% endif %}
                {% endfor %}
            </div>
        </div>
        <div class="footer"></div>
            {% block footer_content %}{% endblock %}
        </div>
    </div>
    
    <script src="/static/js/sidebar.js"></script>
</body>
</html>
